services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: babebar-dev
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - .env.vps
